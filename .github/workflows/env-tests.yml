name: "Env tests"

on:
  push:
    branches:
      - "master"
      - "staging"
    paths:
      - "src/App1/**"
      - "src/App2/**"
      - ".github/workflows/env-tests.yml"

env:
  BUILD_TYPE: TEST

jobs:

  build:
    env: 
      AKS_CLUSTER_NAME: $BUILD_TYPE
      AKS_CLUSTER_RESOURCE_GROUP: kubernetes-$BUILD_TYPE
    runs-on: ubuntu-latest

    steps:
      - name: PRINT
        run: echo ${{ github.ref }}
      - name: PRINT2
        run: echo ${{ env.AKS_CLUSTER_RESOURCE_GROUP }} ${{ env.BUILD_TYPE }}
